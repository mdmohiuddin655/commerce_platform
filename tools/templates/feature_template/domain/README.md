Entities, value objects and transition rules. Pure Dart: no Flutter, no I/O,
no Firebase types. Repository *interfaces* live here; implementations do not.
Server-authoritative rules are mirrored here for UX only — the backend remains
the sole authority (see docs/architecture/layering-rules.md).
