# Shared package: cp_local_store  (directory: packages/local_store)
# Layering rules: docs/architecture/layering-rules.md
name: cp_local_store
description: "Drift-backed local cache and durable queue."
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  cp_contracts:
    path: ../contracts
  cp_core:
    path: ../core
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
