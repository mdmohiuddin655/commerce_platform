# Shared package: cp_contracts  (directory: packages/contracts)
# Layering rules: docs/architecture/layering-rules.md
name: cp_contracts
description: "Shared schemas, exhaustive state transitions and event names (owner: FND-003)."
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  cp_core:
    path: ../core
  meta: ^1.16.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
